# ─── Free mode built-in LLM (server-side, optional) ──────────
# When set, Free mode (no user API key) is powered by this LLM via the
# server-side /api/chat proxy, so the key is never exposed to the browser
# or committed to the repo. If unset, Free mode falls back to the local
# scripted responses (lib/free-coach.ts).
#
# Local dev: copy this file to `.env.local` and fill in real values.
# Production: set these in Vercel → Settings → Environment Variables.

# OpenAI-compatible provider id (e.g. zhipu, openai, deepseek, openai-compatible).
FREE_LLM_PROVIDER=zhipu
# Base URL of the provider's OpenAI-compatible endpoint.
FREE_LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
# Model name to use for Free mode conversations.
FREE_LLM_MODEL=glm-4.5-air
# The API key (KEEP SECRET — never commit the real value).
FREE_LLM_API_KEY=your-key-here

# ─── API TTS built-in (server-side, optional) ────────────────
# When set, the "小米 TTS" voice option synthesizes the AI's replies via the
# server-side /api/tts proxy, so the key is never exposed to the browser. If
# unset (and the user hasn't supplied their own key in settings), TTS falls
# back to the free browser SpeechSynthesis.
# Docs: https://platform.xiaomimimo.com/docs/zh-CN/usage-guide/speech-synthesis-v2.5

# The Xiaomi MiMo TTS API key (KEEP SECRET — never commit the real value).
XIAOMI_TTS_API_KEY=your-key-here
# Optional overrides (sensible defaults are used when unset).
XIAOMI_TTS_BASE_URL=https://api.xiaomimimo.com/v1
XIAOMI_TTS_MODEL=mimo-v2.5-tts

# ─── API ASR built-in (server-side, optional) ────────────────
# When set, the "云端识别" option transcribes recorded audio via the
# server-side /api/asr proxy (SiliconFlow), so the key is never exposed to the
# browser. If unset (and the user hasn't supplied their own key in settings),
# the app uses the free browser Web Speech recognition instead.
# Docs: https://docs.siliconflow.cn/en/api-reference/audio/create-audio-transcriptions

# The SiliconFlow API key (KEEP SECRET — never commit the real value).
SILICONFLOW_ASR_API_KEY=your-key-here
# Optional overrides (sensible defaults are used when unset).
SILICONFLOW_ASR_BASE_URL=https://api.siliconflow.cn/v1
SILICONFLOW_ASR_MODEL=FunAudioLLM/SenseVoiceSmall
